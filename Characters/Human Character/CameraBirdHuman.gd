extends Camera2D

#@export var bird:Node2D
#@export var human:Node2D

# Called every frame. 'delta' is the elapsed time since the previous frame.
func _process(delta):
	pass
	#position.x = (bird.position.x + human.position.x)/2
	#position.y = (bird.position.y + human.position.y)/2
